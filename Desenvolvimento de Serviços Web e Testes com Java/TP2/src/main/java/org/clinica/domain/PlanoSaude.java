package org.clinica.domain;

// Plano de Saúde que define cobertura das consultas
public interface PlanoSaude {

    // Retorna o percentual de cobertura do plano de saúde.
    double getPercentualCobertura();
}
